# Lexcess Clothing — Online build

Static site + admin UI using Supabase directly. No local proxy/server is required.

Deploy the contents of this folder to any static host (for example GitHub Pages).
